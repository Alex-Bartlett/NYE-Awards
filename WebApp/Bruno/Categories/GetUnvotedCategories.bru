meta {
  name: GetUnvotedCategories
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/categories?unvotedBy={{personId}}&round={{round}}
  body: none
  auth: inherit
}

params:query {
  unvotedBy: {{personId}}
  round: {{round}}
}

vars:pre-request {
  personId: 2
  round: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
