meta {
  name: CreateCategory
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/categories
  body: json
  auth: inherit
}

body:json {
  {
    "name": "{{categoryName}}"
  }
}

vars:pre-request {
  categoryName: TestCategory
}

settings {
  encodeUrl: true
  timeout: 0
}
