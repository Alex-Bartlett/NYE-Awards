meta {
  name: CreatePerson
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/people
  body: json
  auth: inherit
}

body:json {
  {
    "name": "TestPerson"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
